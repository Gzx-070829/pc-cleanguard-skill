# START HERE

1. 阅读 `user_summary.md`。
2. 查看 `cleanup_report.md` 和 `pup_insight.md`。
3. 核对 `audit.jsonl` 与 `quarantine_manifest.json`。
4. 真实试用请运行 README 第一屏的 `trial run` 命令。

默认 dry-run；confirm 后默认 quarantine。无联网、无上传、无静默删除。
