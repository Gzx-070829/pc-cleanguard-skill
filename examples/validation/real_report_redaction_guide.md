# Real Report Redaction Guide

使用 `C:\Users\<USER>\...`、`<DEVICE>`、`<EMAIL>`、`<TOKEN>` 替换真实身份信息。不要上传未脱敏 report；本项目不会自动上传。

分享前删除或泛化：用户名、设备名、用户目录、精确安装路径、邮箱、token、序列号和其他个人标识。保留最少必要的 display name、publisher、版本、签名状态与抽象行为元数据。不要上传文件内容。
