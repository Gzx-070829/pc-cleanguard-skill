# Agent Governed Cleanup Model

Agent 可以请求构图、解释、治理 proposal 和请求校验。L0 分析允许；delete/uninstall/disable/registry/service/task/browser mutation 以及未知请求 fail-closed。

L1 仅引用既有 temp/cache/log 安全边界；L2-L4 在 v0.4.0 只设计 proposal；L5 自动执行被阻断。自然语言理由不能提高等级。
