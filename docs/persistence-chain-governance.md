# Persistence Chain Governance / 持久化链路治理

PC CleanGuard 消费显式 report 中的 installed app、startup、service、task、browser、registry-like clue、updater、promo component 与 leftover metadata，生成 L0 图谱。它不真实读取注册表或浏览器，不执行断链。

图谱解释“删不干净”背后的链路，但不是定罪或执行授权。名称、发布者和路径近似必须降级；任何未来处理都必须用户确认、可回滚、可审计。
