# v0.4.0 Safety Boundaries

图谱只消费显式 report。浏览器与 registry-like clue 不触发真实扫描。L2 quarantine、L3 official uninstaller、L4 registry/service/task/browser governance 均为未来 proposal，本版本不执行。PUP evidence 不能单独触发任何动作，Agent 请求不能提升权限。
